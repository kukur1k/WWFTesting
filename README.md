# DevProjectManager
## Описание
Консольное приложение DevProjectManager предназначено для работы с рабочими проектами и ведения их списка. Основная функция - чтение данных о проектах и запись их в файл из различных форматов (JSON, YAML, XML, CSV). Присутствует  функционал для добавления, изменения, сортировки и поиска проектов (с фильтрацией по свойствам). 
## Класс `DevProject` - основная модель данных проекта
### Конструктор `DevProject(string name, string leader, Status status, int priority)` 
- инициализирует объект DevProject (Сам проект)
### `ResetID() `
- сбрасывает счетчик событий
### `override string ToString()`
- переопределенный метод для вывода проекта
### ` SetCurrentID(int devProjectID)`
- метод для присвоения значения индексу (используется при пересчете нового индекса после чтения из файла)
### Класс `Projects` - хранит список проектов (объекты типа DevProject)
### Класс `WorkwithData<T>` - универсальный класс с методами для сериализации/десериализации
###  `WriteToFile<T>(List<T> obj, string filePath)`
- Универсальный метод для записи в файл
- Параметры:
  - `obj`: список объектов типа Т для записи
  - `filePath`: путь к файлу
### `ReadData(string filepath)`
- Универсальный метод для чтения из файла
- Параметры:
  - `filePath`: путь к файлу
## Приватные методы для разных типов файлов
### Чтение:
### `ReadFromJson<T>(string filePath)`
- Универсальный метод для чтения данных и десериализации в список объектов типа Т из JSON
- Параметры:
  - `filePath`: путь к файлу
### `ReadFromXML<T>(string filePath)`
- Универсальный метод для чтения данных и десериализации в список объектов типа Т из XML
- Параметры:
  - `filePath`: путь к файлу
### `ReadFromCsv<T>(string filePath)`
- Универсальный метод для чтения данных и десериализации в список объектов типа Т из CSV
- Параметры:
  - `filePath`: путь к файлу

### Запись:
### `WriteToJson<T>(List<T> obj, string filePath)`
- Универсальный метод для сереализации списка объектов типа Т в JSON и записи в файл
- Параметры:
  - `filePath`: путь к файлу
  - `obj`: список объектов типа Т для записи
### `WriteToXML<T>(List<T> obj, string filePath)`
- Универсальный метод для сереализации списка объектов типа Т в XML и записи в файл
- Параметры:
  - `filePath`: путь к файлу
  - `obj`: список объектов типа Т для записи
### `WriteToCSV<T>(List<T> obj, string filePath)`
- Универсальный метод для сереализации списка объектов типа Т в CSV и записи в файл
- Параметры:
  - `filePath`: путь к файлу
  - `obj`: список объектов типа Т для записи
### `WriteToYaml<T>(List<T> obj, string filePath)`
- Универсальный метод для сереализации списка объектов типа Т в YAML и записи в файл
- Параметры:
  - `filePath`: путь к файлу
  - `obj`: список объектов типа Т для записи
